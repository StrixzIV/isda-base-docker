# Question 54: High Average Salary Departments

**Topic Covered**: `HAVING`

## Database Schema Reference

### Table: `employees`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `employee_id` | `INT` | PRIMARY KEY | Unique identifier for employee |
| `first_name` | `VARCHAR(50)` | NOT NULL | Employee given name |
| `last_name` | `VARCHAR(50)` | NOT NULL | Employee family name |
| `department_id` | `INT` | FOREIGN KEY (`departments.department_id`) | Assigned department ID |
| `salary` | `DECIMAL(10, 2)` | NOT NULL | Annual salary amount |
| `hire_date` | `DATE` | NOT NULL | Date employee was hired |
| `job_title` | `VARCHAR(50)` | NOT NULL | Official job title |

---

## Problem Statement

Find departments with high average salary budgets. Group employees by department ID and filter for departments where the average employee salary is strictly greater than $75,000.00. Display the department ID and average salary as `avg_salary`, sorted from highest average salary to lowest.

## Expected Output Columns

| department_id | avg_salary |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/54.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
