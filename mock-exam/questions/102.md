# Question 102: Line Items Above Average Quantity

**Topic Covered**: `SUBQUERY`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Find order line items where the quantity ordered is strictly greater than the average quantity per line item across all order items. Display item ID, product name, quantity, and unit price. Sort by quantity descending, then by item ID ascending.

## Expected Output Columns

| item_id | product_name | quantity | unit_price |
| :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/102.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
