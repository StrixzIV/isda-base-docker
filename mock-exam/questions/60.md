# Question 60: Master Challenge: High Revenue Departments

**Topic Covered**: `HAVING`

## Database Schema Reference

### Table: `departments`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `department_id` | `INT` | PRIMARY KEY | Unique identifier for department |
| `department_name` | `VARCHAR(50)` | NOT NULL | Department name (e.g. Sales, Engineering) |
| `location` | `VARCHAR(50)` | NOT NULL | Office city location |

### Table: `employees`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `employee_id` | `INT` | PRIMARY KEY | Unique identifier for employee |
| `first_name` | `VARCHAR(50)` | NOT NULL | Employee given name |
| `last_name` | `VARCHAR(50)` | NOT NULL | Employee family name |
| `department_id` | `INT` | FOREIGN KEY (`departments.department_id`) | Assigned department ID |
| `salary` | `DECIMAL(10, 2)` | NOT NULL | Annual salary amount |
| `hire_date` | `DATE` | NOT NULL | Date employee was hired |
| `job_title` | `VARCHAR(50)` | NOT NULL | Official job title |

### Table: `orders`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `order_id` | `INT` | PRIMARY KEY | Unique identifier for order |
| `customer_id` | `INT` | FOREIGN KEY (`customers.customer_id`) | Customer who placed order |
| `employee_id` | `INT` | FOREIGN KEY (`employees.employee_id`) | Employee who handled order |
| `order_date` | `DATE` | NOT NULL | Date order was placed |
| `status` | `VARCHAR(20)` | NOT NULL | Order status (`Completed`, `Pending`, `Cancelled`) |
| `total_amount` | `DECIMAL(10, 2)` | NOT NULL | Total monetary order value |

---

## Problem Statement

Identify high-performing departments with multiple active representatives and high sales volume. Combine department, staff, and order records, group by department ID and department name, and filter for departments that have at least 2 distinct employees handling orders and a total order revenue strictly exceeding $5,000.00. Display department ID, department name, employee count as `emp_count`, and total department revenue as `dept_revenue`, sorted by department revenue descending.

## Expected Output Columns

| department_id | department_name | emp_count | dept_revenue |
| :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/60.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
