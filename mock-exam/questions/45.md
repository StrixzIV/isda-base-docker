# Question 45: Orders with Customer and Employee Names

**Topic Covered**: `JOIN`

## Database Schema Reference

### Table: `orders`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `order_id` | `INT` | PRIMARY KEY | Unique identifier for order |
| `customer_id` | `INT` | FOREIGN KEY (`customers.customer_id`) | Customer who placed order |
| `employee_id` | `INT` | FOREIGN KEY (`employees.employee_id`) | Employee who handled order |
| `order_date` | `DATE` | NOT NULL | Date order was placed |
| `status` | `VARCHAR(20)` | NOT NULL | Order status (`Completed`, `Pending`, `Cancelled`) |
| `total_amount` | `DECIMAL(10, 2)` | NOT NULL | Total monetary order value |

### Table: `customers`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `customer_id` | `INT` | PRIMARY KEY | Unique identifier for customer |
| `customer_name` | `VARCHAR(100)` | NOT NULL | Full customer or corporate name |
| `city` | `VARCHAR(50)` | NOT NULL | Customer city location |
| `country` | `VARCHAR(50)` | NOT NULL | Customer country location |
| `registration_date` | `DATE` | NOT NULL | Account registration date |

### Table: `employees`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `employee_id` | `INT` | PRIMARY KEY | Unique identifier for employee |
| `first_name` | `VARCHAR(50)` | NOT NULL | Employee given name |
| `last_name` | `VARCHAR(50)` | NOT NULL | Employee family name |
| `department_id` | `INT` | FOREIGN KEY (`departments.department_id`) | Assigned department ID |
| `salary` | `DECIMAL(10, 2)` | NOT NULL | Annual salary amount |
| `hire_date` | `DATE` | NOT NULL | Date employee was hired |
| `job_title` | `VARCHAR(50)` | NOT NULL | Official job title |

---

## Problem Statement

Produce an order report showing who placed the order and which sales representative handled it. Display the order ID, customer name, handling employee's full name (first name and last name separated by a space) as `employee_name`, and order total amount. Sort by order ID in ascending order.

## Expected Output Columns

| order_id | customer_name | employee_name | total_amount |
| :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/45.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
