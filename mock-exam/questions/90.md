# Question 90: City Customer & Country Geographic Summary

**Topic Covered**: `GROUP BY`

## Database Schema Reference

### Table: `customers`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `customer_id` | `INT` | PRIMARY KEY | Unique identifier for customer |
| `customer_name` | `VARCHAR(100)` | NOT NULL | Full customer or corporate name |
| `city` | `VARCHAR(50)` | NOT NULL | Customer city location |
| `country` | `VARCHAR(50)` | NOT NULL | Customer country location |
| `registration_date` | `DATE` | NOT NULL | Account registration date |

---

## Problem Statement

Group customers by city and display the city name, customer count as `customer_count`, and count of unique countries represented in that city as `country_count`. Sort alphabetically by city.

## Expected Output Columns

| city | customer_count | country_count |
| :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/90.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
