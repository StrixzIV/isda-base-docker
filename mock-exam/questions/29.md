# Question 29: Order Financial Metrics Summary

**Topic Covered**: `AGGREGATION`

## Database Schema Reference

### Table: `orders`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `order_id` | `INT` | PRIMARY KEY | Unique identifier for order |
| `customer_id` | `INT` | FOREIGN KEY (`customers.customer_id`) | Customer who placed order |
| `employee_id` | `INT` | FOREIGN KEY (`employees.employee_id`) | Employee who handled order |
| `order_date` | `DATE` | NOT NULL | Date order was placed |
| `status` | `VARCHAR(20)` | NOT NULL | Order status (`Completed`, `Pending`, `Cancelled`) |
| `total_amount` | `DECIMAL(10, 2)` | NOT NULL | Total monetary order value |

---

## Problem Statement

Produce a comprehensive financial summary across all orders. Calculate the total count of orders as `total_orders`, grand total order value as `grand_total`, average order value as `avg_amount`, minimum order value as `min_amount`, and maximum order value as `max_amount`.

## Expected Output Columns

| total_orders | grand_total | avg_amount | min_amount | max_amount |
| :--- | :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/29.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
