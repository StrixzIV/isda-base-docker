# Question 17: Job Title Keyword Search

**Topic Covered**: `WHERE`

## Database Schema Reference

### Table: `employees`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `employee_id` | `INT` | PRIMARY KEY | Unique identifier for employee |
| `first_name` | `VARCHAR(50)` | NOT NULL | Employee given name |
| `last_name` | `VARCHAR(50)` | NOT NULL | Employee family name |
| `department_id` | `INT` | FOREIGN KEY (`departments.department_id`) | Assigned department ID |
| `salary` | `DECIMAL(10, 2)` | NOT NULL | Annual salary amount |
| `hire_date` | `DATE` | NOT NULL | Date employee was hired |
| `job_title` | `VARCHAR(50)` | NOT NULL | Official job title |

---

## Problem Statement

Find all staff members working in customer-facing representative or specialist roles. Retrieve the employee ID, first name, last name, and job title for employees whose job title contains either 'Representative' or 'Specialist'. Sort by employee ID in ascending order.

## Expected Output Columns

| employee_id | first_name | last_name | job_title |
| :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/17.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
