# Question 37: Product Sales Volume Summary

**Topic Covered**: `GROUP BY`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Analyze total sales volume and gross revenue for every individual product. Group order items by product name, showing the product name, total units sold as `total_units`, and total revenue generated as `product_revenue`. Sort by total product revenue in descending order.

## Expected Output Columns

| product_name | total_units | product_revenue |
| :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/37.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
