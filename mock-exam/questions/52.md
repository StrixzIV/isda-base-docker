# Question 52: High Volume Categories

**Topic Covered**: `HAVING`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Identify top-selling product categories based on bulk volume. Group order items by category and find categories where the total number of items sold exceeds 5 units. Display the category name and total quantity sold as `total_quantity`, sorted by total quantity from highest to lowest.

## Expected Output Columns

| category | total_quantity |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/52.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
