# Question 20: Complex Department & Salary Filter

**Topic Covered**: `WHERE`

## Database Schema Reference

### Table: `employees`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `employee_id` | `INT` | PRIMARY KEY | Unique identifier for employee |
| `first_name` | `VARCHAR(50)` | NOT NULL | Employee given name |
| `last_name` | `VARCHAR(50)` | NOT NULL | Employee family name |
| `department_id` | `INT` | FOREIGN KEY (`departments.department_id`) | Assigned department ID |
| `salary` | `DECIMAL(10, 2)` | NOT NULL | Annual salary amount |
| `hire_date` | `DATE` | NOT NULL | Date employee was hired |
| `job_title` | `VARCHAR(50)` | NOT NULL | Official job title |

---

## Problem Statement

Retrieve staff records for employees assigned to department 101, as well as senior team members in department 102 who earn $120,000.00 or more per year. Display the employee ID, first name, last name, department ID, and salary, ordered by employee ID in increasing order.

## Expected Output Columns

| employee_id | first_name | last_name | department_id | salary |
| :--- | :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/20.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
