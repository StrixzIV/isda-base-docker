# Question 69: Average Customer Total Expenditure

**Topic Covered**: `SUBQUERY`

## Database Schema Reference

### Table: `orders`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `order_id` | `INT` | PRIMARY KEY | Unique identifier for order |
| `customer_id` | `INT` | FOREIGN KEY (`customers.customer_id`) | Customer who placed order |
| `employee_id` | `INT` | FOREIGN KEY (`employees.employee_id`) | Employee who handled order |
| `order_date` | `DATE` | NOT NULL | Date order was placed |
| `status` | `VARCHAR(20)` | NOT NULL | Order status (`Completed`, `Pending`, `Cancelled`) |
| `total_amount` | `DECIMAL(10, 2)` | NOT NULL | Total monetary order value |

---

## Problem Statement

Calculate the overall average spending per customer across all customers who have placed completed orders. Calculate each customer's total completed spending in a subquery, then return the single overall average spending value labeled as `avg_customer_spending`.

## Expected Output Columns

| avg_customer_spending |
| :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/69.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
