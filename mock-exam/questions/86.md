# Question 86: Account Registrations by Year

**Topic Covered**: `GROUP BY`

## Database Schema Reference

### Table: `customers`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `customer_id` | `INT` | PRIMARY KEY | Unique identifier for customer |
| `customer_name` | `VARCHAR(100)` | NOT NULL | Full customer or corporate name |
| `city` | `VARCHAR(50)` | NOT NULL | Customer city location |
| `country` | `VARCHAR(50)` | NOT NULL | Customer country location |
| `registration_date` | `DATE` | NOT NULL | Account registration date |

---

## Problem Statement

Group customers by registration year and calculate the total number of accounts created each year as `account_count`. Display registration year as `reg_year` and customer count, sorted chronologically by registration year.

## Expected Output Columns

| reg_year | account_count |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/86.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
