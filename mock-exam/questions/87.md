# Question 87: Category Line Items & Revenue Breakdown

**Topic Covered**: `GROUP BY`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Summarize product category performance. For each category, display category name, number of line items as `item_count`, total units sold as `total_units`, and total revenue as `total_revenue`. Sort by total revenue from highest to lowest.

## Expected Output Columns

| category | item_count | total_units | total_revenue |
| :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/87.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
