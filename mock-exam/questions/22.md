# Question 22: Total Payroll & Average Salary

**Topic Covered**: `AGGREGATION`

## Database Schema Reference

### Table: `employees`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `employee_id` | `INT` | PRIMARY KEY | Unique identifier for employee |
| `first_name` | `VARCHAR(50)` | NOT NULL | Employee given name |
| `last_name` | `VARCHAR(50)` | NOT NULL | Employee family name |
| `department_id` | `INT` | FOREIGN KEY (`departments.department_id`) | Assigned department ID |
| `salary` | `DECIMAL(10, 2)` | NOT NULL | Annual salary amount |
| `hire_date` | `DATE` | NOT NULL | Date employee was hired |
| `job_title` | `VARCHAR(50)` | NOT NULL | Official job title |

---

## Problem Statement

Compute company-wide payroll statistics. Calculate the total annual salary expenditure labeled as `total_payroll` and the average employee salary labeled as `avg_salary` across all employees.

## Expected Output Columns

| total_payroll | avg_salary |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/22.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
