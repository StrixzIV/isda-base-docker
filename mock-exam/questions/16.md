# Question 16: Bulk Low-Cost Items Filter

**Topic Covered**: `WHERE`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Find bulk purchases of budget products. Display the item ID, product name, unit price, and quantity for items ordered in quantities of 5 or more units where the unit price is under $200.00. Sort by quantity from highest to lowest.

## Expected Output Columns

| item_id | product_name | unit_price | quantity |
| :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/16.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
