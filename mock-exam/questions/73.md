# Question 73: Customer Directory Tier Projection

**Topic Covered**: `SELECT`

## Database Schema Reference

### Table: `customers`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `customer_id` | `INT` | PRIMARY KEY | Unique identifier for customer |
| `customer_name` | `VARCHAR(100)` | NOT NULL | Full customer or corporate name |
| `city` | `VARCHAR(50)` | NOT NULL | Customer city location |
| `country` | `VARCHAR(50)` | NOT NULL | Customer country location |
| `registration_date` | `DATE` | NOT NULL | Account registration date |

---

## Problem Statement

Generate a customer directory showing customer ID, customer name, city, country, and include a static column labeled `tier` containing the text value `'Standard'` for all accounts. Sort by customer ID in increasing order.

## Expected Output Columns

| customer_id | customer_name | city | country | tier |
| :--- | :--- | :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/73.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
