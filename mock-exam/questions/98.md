# Question 98: High Gross Revenue Product Categories

**Topic Covered**: `HAVING`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Group order items by category and filter for categories where total gross dollar volume (`SUM(unit_price * quantity)`) exceeds $3,000.00. Display category name and gross revenue as `gross_revenue`, sorted by gross revenue descending.

## Expected Output Columns

| category | gross_revenue |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/98.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
