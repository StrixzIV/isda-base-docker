# Question 19: Hardware Price Range Filter

**Topic Covered**: `WHERE`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Find hardware products priced in the mid-range bracket. Retrieve the item ID, product name, and unit price for items in the 'Hardware' category costing between $50.00 and $300.00 (inclusive). Sort by unit price from lowest to highest.

## Expected Output Columns

| item_id | product_name | unit_price |
| :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/19.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
