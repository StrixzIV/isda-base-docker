# Question 57: Premium Product Categories

**Topic Covered**: `HAVING`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Identify premium product categories with high average pricing. Group order items by category and filter for categories where the average unit price is $100.00 or higher. Display the category name and average price as `avg_price`, sorted by average price descending.

## Expected Output Columns

| category | avg_price |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/57.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
