# Question 115: Presidents Count Born per State (Textbook Ex 9.02)

**Topic Covered**: `GROUP BY`

## Database Schema Reference

### Table: `president`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `pres_name` | `VARCHAR(50)` | PRIMARY KEY | President full name |
| `birth_yr` | `INT` | NOT NULL | Birth year |
| `yrs_serv` | `INT` | NOT NULL | Total years served in office |
| `death_age` | `INT` | NULLABLE | Age at death (NULL if living) |
| `party` | `VARCHAR(30)` | NOT NULL | Political party |
| `state_born` | `VARCHAR(30)` | NOT NULL | State of birth |

---

## Problem Statement

For each state, find the total number of presidents who were born in that state. Display state name as `state_born` and count of presidents as `pres_count`. Sort by pres_count descending, then by state_born ascending.

## Expected Output Columns

| state_born | pres_count |
| :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/115.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
