# Question 72: Product Discount Price Projection

**Topic Covered**: `SELECT`

## Database Schema Reference

### Table: `order_items`

| Column Name | Data Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `item_id` | `INT` | PRIMARY KEY | Unique identifier for item record |
| `order_id` | `INT` | FOREIGN KEY (`orders.order_id`) | Associated order ID |
| `product_name` | `VARCHAR(100)` | NOT NULL | Name of product |
| `category` | `VARCHAR(50)` | NOT NULL | Product category (`Hardware`, `Software`, `Services`) |
| `unit_price` | `DECIMAL(10, 2)` | NOT NULL | Price per single unit |
| `quantity` | `INT` | NOT NULL | Quantity ordered |

---

## Problem Statement

Calculate a 15% promotional discount on all product unit prices. Display product name, original unit price, and discounted unit price labeled as `discounted_price` (`unit_price * 0.85`). Sort by discounted price from highest to lowest.

## Expected Output Columns

| product_name | unit_price | discounted_price |
| :--- | :--- | :--- |

## Answer File Location

Write your Standard ANSI SQL query in `mock-exam/answers/72.sql.txt` (or `mock-exam/submission.sql.txt` for active 42 exam runs).
